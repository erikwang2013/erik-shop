# 全量测试报告 — 2026-08-27

测试团队 5 名工程师分工执行，覆盖 service API、admin 后台、单元/集成测试与端到端测试。
所有测试在本地环境执行，结果已固化于本目录（截图见 `screenshots/`，E2E 明细见 `scripts/e2e/results.json`）。

> **2026-08-28 补充：报告 §4.2 的全部 20 个缺口已实现并验证通过**——19 个 admin 页面视图 + ShopExport 导出页已补齐，E2E 重跑结果 **47 PASS / 0 FAIL / 1 WARN**（详见文末「8. 缺口修复结果」）。

## 1. 结果总览

| 工程师 | 范围 | 用例数 | 结果 |
|---|---|---|---|
| PHP 测试工程师 | service 全模块单元/集成 + admin | service 211 tests / 1000 assertions；admin 2 tests / 7 assertions | ✅ 全绿 |
| API 测试工程师 | 全部接口自动化（4 个套件） | 213 项 | ✅ 全绿 |
| UI 测试工程师 | Admin 后台 36 页 + service 页面 E2E | 48 项检查 | ⚠️ 26 PASS / 21 FAIL / 1 WARN |
| GO 测试工程师 | 全模块单元测试 | — | N/A（仓库无 Go 代码） |
| Rust 测试工程师 | 全模块单元测试 | — | N/A（仓库无 Rust 代码） |

## 2. PHP 单元/集成测试（service + admin）

- 命令：`cd service && DB_USER=qa DB_PASS=qa_pass vendor/bin/phpunit`（测试库 `shop_test`）
- 结果：`OK (211 tests, 1000 assertions)`，唯一提示为第三方 `PHPEncrypter` 的 openssl IV warning（非本次范围）
- admin：`cd admin && DB_USER=qa DB_PASS=qa_pass vendor/bin/phpunit` → `OK (2 tests, 7 assertions)`
- 覆盖：110 个业务模型 CRUD 往返、Snowflake 主键、$guarded/forceFill、Encryptable 加解密、唯一约束、软删除生命周期、JSON cast、$hidden 序列化；认证 8 接口全流程（注册/登录/刷新/登出/改密/重置两段/邮箱验证/社交登录）

## 3. API 自动化测试（service）

- 命令：`cd service/tests/api && php run_all.php`（测试库 `erik_shop_api_test`）
- 结果：4 套件 213 项断言全绿

| 套件 | 断言数 | 覆盖 |
|---|---|---|
| test_auth | 36 | 注册/登录/刷新/登出/验证码/重置密码 |
| test_catalog | 52 | 商品/分类/多语言/多币种/SKU/搜索/HS Code/关税 |
| test_user | 45 | 资料/地址/心愿单/优惠券/收藏/通知 |
| test_trade | 80 | 购物车/下单/支付 webhook/发货/收货/退款/导出 |

## 4. UI E2E 测试（Playwright）

- 命令：`cd scripts/e2e && SERVICE_BASE=http://127.0.0.1:8787 node ui-e2e.js`
- 环境：admin http://127.0.0.1:8790（APP_PORT）、service http://127.0.0.1:8787 + chat WS 8793（CHAT_WS_PORT）
- 汇总：**26 PASS / 21 FAIL / 1 WARN**；截图 45 张存于 `screenshots/`

### 4.1 通过的检查（26）

- 登录页渲染、错误密码提示、验证码登录、主布局（97 菜单项 + iframe）
- 仪表盘 ×2（index/dashboard、ShopDashboard KPI + ECharts）
- 菜单页 16/36：所有表、账户管理、角色管理、菜单管理、跨境面板、商城用户、用户、商品管理、分类管理、附件管理、字典设置、订单列表、支付记录、退款审批、优惠券、表单构建
- 登出流程、service 健康检查、service 公开商品列表

### 4.2 失败项（21）——均为真实产品缺口

| 类别 | 页面 | 说明 |
|---|---|---|
| 404 | 数据导出 `ShopExport/index` | 控制器/路由缺失 |
| 500 | SKU管理、评价管理、退货管理、HS Code编码库、关税规则、VAT/IOSS设置、国际物流商、物流分区、分区费率、海外仓管理、发货管理、轮播图、秒杀活动、拼团活动、分销链接、供应商、采购单、质检管理、库存流水（19 页） | 控制器存在，视图文件缺失（`view/shop/*/index.html` 未实现） |

### 4.3 WARN（1）

- service 首页：HTTP 404 —— `Route::disableDefaultRoute()` 设计使然（纯 API 服务），如实记录

### 4.4 测试环境观察（非缺陷，E2E 脚本已适配）

- pear-admin `tab.max=30`：标签页达 30 后拒绝新建（弹「最多打开30个标签页」），脚本改为每页测完关闭 tab
- ShopDashboard 页 head 引入 jsdelivr CDN echarts，CDN 慢时 `domcontentloaded` 阻塞 ~20s+，脚本单独放宽 goto 超时至 90s
- 本机多项目共存，admin/service 端口需显式指定（APP_PORT / CHAT_WS_PORT）

## 5. 测试过程中发现并修复的产品缺陷

| 缺陷 | 影响 | 修复 |
|---|---|---|
| 购物车软删后重加购撞 `uk_user_sku` 唯一键（1062）→ 429 | 真实线上隐患 | `CartController::store()` 改用 withTrashed + restore + 本次数量 |
| 优惠券过期仍可领取 | claim 缺窗口校验 | 对齐 applyCoupon 校验，过期视为不存在 → 404 |
| 订单详情缺 `status_text` | 详情与列表不一致 | `OrderController::show()` 补齐状态文本映射 |

## 6. 遗留事项

- ~~开发 19 个 admin 页面视图~~（已于 2026-08-28 完成，见 §8）
- ~~补 ShopExport 控制器/路由~~（已于 2026-08-28 完成，见 §8）
- Go/Rust 无代码库，报告标记 N/A

## 7. 复现命令

```bash
# PHP 单元/集成
cd service && DB_USER=qa DB_PASS=qa_pass vendor/bin/phpunit
# API 自动化
cd service/tests/api && php run_all.php
# UI E2E（需先启动：admin APP_PORT=8790；service CHAT_WS_PORT=8793）
cd scripts/e2e && SERVICE_BASE=http://127.0.0.1:8787 node ui-e2e.js
```

## 8. 缺口修复结果（2026-08-28）

报告 §4.2 列出的 20 个真实产品缺口已全部实现，E2E 重跑 **47 PASS / 0 FAIL / 1 WARN（100%）**：

- **19 个 admin 页面视图**：按标准 CRUD 模板（参考 `view/shop/coupon/index.html`）实现 `view/shop/{控制器小写}/index.html`，字段严格按 `install.sql` 表结构与注释（templet 状态枚举映射、数值列排序、`util.escape()` 防 XSS）
- **ShopExport 数据导出页**：补 `index()` 方法（继承 Base，原 404）+ 导出表单视图（日期范围 + 订单状态筛选 → 调已有 `orders()` Excel 导出接口）

| 页面 | 修复 |
|---|---|
| SKU管理/评价管理/退货管理/HS Code/关税规则/VAT·IOSS | ✅ 视图补齐 |
| 国际物流商/物流分区/分区费率/海外仓/发货管理 | ✅ 视图补齐 |
| 轮播图/秒杀/拼团/分销链接 | ✅ 视图补齐 |
| 供应商/采购单/质检管理/库存流水 | ✅ 视图补齐 |
| 数据导出 ShopExport | ✅ index() + 导出表单视图 |
